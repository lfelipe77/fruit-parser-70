// Legacy hash redirect script - DISABLED to prevent routing conflicts
// Hash routing (#/) is no longer supported for ganhavel URLs
// Only clean URLs (/ganhavel/slug) and .html meta URLs are supported
(function () {
  console.log('[REDIRECT-HASH] Legacy script disabled - clean URLs only');
})();
