# Ficha de Página — <NOME DA PÁGINA>

## Leitura (READ)
- **Tabelas/Views:**
- **Colunas usadas:**
- **Filtros (where):**
- **Ordem/Limite:**
- **Realtime?** (sim/não)

## Escrita (WRITE)
- **Tabelas:**
- **Operações** (insert/update/delete):
- **Payload (shape):**
- **RLS esperado:**

## Elementos de UI ↔ Dados
- `<Bloco/Componente>` → `<campo(s) no DB>` (fallback se null)
- ...

## Mocks / Estado Local
- **`<item>`**: por enquanto local (não persiste). Plano de conexão: `<tabela/endpoint>`

## Regras de Visibilidade
- **Público / auth / admin**
- **Comportamentos com usuário não logado**

## Erros e Fallbacks
- **Campos que podem vir null e o que mostrar**

## Telemetria / Logs (opcional)
- **onde logar eventos**

## Dívida Técnica / TODO
- **Itens a conectar depois**